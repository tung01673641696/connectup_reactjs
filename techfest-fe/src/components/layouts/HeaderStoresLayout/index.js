import classNames from "classnames/bind";
import styles from './HeaderStoresLayout.module.scss'

const cx = classNames.bind(styles)
function HeaderStoresLayout() {
    return (
        <div>

        </div>
    );
}

export default HeaderStoresLayout;